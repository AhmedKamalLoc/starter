import { Meta, Story } from '@storybook/addon-docs';

import { Avatar } from './avatar';

<Meta title="Components/Avatar" component={Avatar} />

# Avatar Component

The `Avatar` component is used to display user profile pictures with a fallback for when no image is
provided.

## Usage

```tsx
import { Avatar } from '@/components/avatar/avatar';

<Avatar src="https://example.com/profile.jpg" alt="User Name" />;
```

## Props

| Prop        | Type   | Description                        | Default |
| ----------- | ------ | ---------------------------------- | ------- |
| `src`       | string | The source URL of the avatar image | -       |
| `alt`       | string | Alt text for the avatar image      | -       |
| `className` | string | Additional CSS classes for styling | -       |

## Examples

### Default Avatar

<Story name="Default" args={{ src: 'https://picsum.photos/200', alt: 'User Avatar' }}>
  {(args) => <Avatar {...args} />}
</Story>

### Fallback Avatar

<Story name="Fallback" args={{ src: '', alt: 'John Doe' }}>
  {(args) => <Avatar {...args} />}
</Story>
